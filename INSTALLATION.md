# دليل التثبيت السريع - n8n مع Cloudflare Tunnel

## 📦 محتويات الحزمة

هذه الحزمة تحتوي على كل ما تحتاجه لتشغيل n8n مع Cloudflare Tunnel:

```
n8n/
├── README.md                    # الدليل الشامل الكامل
├── INSTALLATION.md              # هذا الملف - دليل التثبيت السريع
├── docker-compose.yml           # ملف Docker Compose
├── .env.example                 # نموذج المتغيرات البيئية
├── .gitignore                   # حماية الملفات الحساسة
│
├── install-requirements.sh      # تثبيت المتطلبات (Linux/macOS)
├── quick-start.sh               # بدء سريع (Linux/macOS)
├── get-url.sh                   # الحصول على رابط Tunnel (Linux/macOS)
│
├── quick-start.ps1              # بدء سريع (Windows)
├── get-url.ps1                  # الحصول على رابط Tunnel (Windows)
│
├── cloudflare/                  # مجلد تكوين Cloudflare
└── n8n-data/                    # مجلد بيانات n8n (يُنشأ تلقائياً)
```

---

## ⚡ التثبيت السريع (3 خطوات فقط!)

### على Linux/macOS:

```bash
# 1. فك ضغط الحزمة
unzip n8n-cloudflare-tunnel.zip
cd n8n

# 2. تثبيت المتطلبات
./install-requirements.sh

# 3. تحميل صور Docker مسبقاً (مرة واحدة فقط)
./download-images.sh

# 4. تشغيل n8n
./quick-start.sh
```

### على Windows:

```powershell
# 1. فك ضغط الحزمة
# انقر بالزر الأيمن → Extract All

# 2. افتح PowerShell في المجلد

# 3. تثبيت Docker Desktop يدوياً من:
# https://www.docker.com/products/docker-desktop/

# 4. تشغيل n8n
.\quick-start.ps1
```

---

## 📋 المتطلبات الأساسية

### Linux/macOS:
- Docker
- Docker Compose

**تثبيت تلقائي:**
```bash
./install-requirements.sh
```

### Windows:
- [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)

---

## 🚀 دليل التشغيل خطوة بخطوة

### الخطوة 1: فك الضغط

افك ضغط ملف `n8n-cloudflare-tunnel.zip` إلى المكان الذي تريده.

### الخطوة 2: تثبيت المتطلبات

#### Linux/macOS:
```bash
cd n8n
./install-requirements.sh
```

السكريبت سيقوم بـ:
- ✅ التحقق من تثبيت Docker
- ✅ تثبيت Docker إذا لم يكن مثبتاً
- ✅ تثبيت Docker Compose
- ✅ إعداد الصلاحيات اللازمة

#### Windows:
1. حمّل وثبّت [Docker Desktop](https://www.docker.com/products/docker-desktop/)
2. شغّل Docker Desktop
3. تأكد من تفعيل WSL 2

### الخطوة 3: تحميل صور Docker مسبقاً ⭐

**هذه خطوة جديدة مهمة - تحميل n8n و Cloudflare مسبقاً!**

#### Linux/macOS:
```bash
./download-images.sh
```

#### Windows:
```powershell
# انقر بالزر الأيمن على download-images.ps1 → Run with PowerShell
# أو:
.\download-images.ps1
```

**سيتم تحميل:**
- ✅ n8n (~300MB)
- ✅ Cloudflare Tunnel (~50MB)

**ملاحظة:** هذا يحدث **مرة واحدة فقط**. بعدها التشغيل سيكون فورياً!

⏱️ **الوقت المتوقع:** 5-10 دقائق (حسب سرعة الإنترنت)

---

### الخطوة 4: اختر نوع Tunnel

لديك **خياران**:

#### ✨ الخيار 1: Quick Tunnel (موصى به - مجاني 100%)

- ✅ **بدون domain** - لا تحتاج لشراء أي شيء
- ✅ **بدون حساب** - لا تحتاج حتى حساب Cloudflare
- ✅ **HTTPS آمن** - مقبول من Telegram وجميع الخدمات
- ⚠️ الرابط يتغير عند إعادة التشغيل

**للتشغيل:**
```bash
./quick-start.sh
# اختر: 1 (Quick Tunnel)
```

**للحصول على الرابط:**
```bash
./get-url.sh
```

---

#### 🌐 الخيار 2: Named Tunnel (للمحترفين)

- ✅ **رابط ثابت** - لا يتغير أبداً
- ✅ **نطاق مخصص** - مثل: `n8n.yourdomain.com`
- ⚠️ يحتاج domain وحساب Cloudflare

**خطوات إضافية:**

1. سجل حساب على [Cloudflare](https://dash.cloudflare.com)
2. أضف نطاقك إلى Cloudflare
3. اذهب إلى: **Zero Trust** → **Networks** → **Tunnels**
4. أنشئ Tunnel جديد واحصل على التوكن
5. انسخ `.env.example` إلى `.env`
6. ضع التوكن في `.env`
7. شغّل: `./quick-start.sh` واختر `2`

---

### الخطوة 4: الوصول إلى n8n

#### مع Quick Tunnel:

احصل على الرابط:
```bash
./get-url.sh
```

ستحصل على رابط مثل:
```
https://abc-xyz-123.trycloudflare.com
```

افتحه في المتصفح!

#### مع Named Tunnel:

افتح:
```
https://n8n.yourdomain.com
```

---

## 🔐 بيانات الدخول

افتح ملف `.env` لرؤية:
- اسم المستخدم: `admin` (افتراضي)
- كلمة المرور: (غيّرها في `.env`)

---

## 🔧 الأوامر الأساسية

### تشغيل الخدمات

```bash
# Quick Tunnel
docker-compose --profile quick-tunnel up -d

# Named Tunnel
docker-compose --profile named-tunnel up -d
```

### إيقاف الخدمات

```bash
# Quick Tunnel
docker-compose --profile quick-tunnel down

# Named Tunnel
docker-compose --profile named-tunnel down
```

### الحصول على الرابط (Quick Tunnel)

```bash
./get-url.sh
```

### مشاهدة السجلات

```bash
docker-compose logs -f
```

---

## 🌍 نقل الحزمة لنظام آخر

ببساطة:
1. انسخ المجلد الكامل إلى النظام الجديد
2. ثبّت المتطلبات (`install-requirements.sh` أو Docker Desktop)
3. شغّل `quick-start.sh`

**يعمل على:**
- ✅ Ubuntu/Debian/Linux
- ✅ macOS
- ✅ Windows 10/11

---

## 💾 النسخ الاحتياطي

احفظ هذه المجلدات:
- `n8n-data/` - جميع workflows والبيانات
- `.env` - الإعدادات الخاصة بك

```bash
tar -czf backup.tar.gz n8n-data/ .env
```

---

## ❓ الأسئلة الشائعة

### هل الحزمة مجانية؟
✅ نعم، 100% مجانية! (Quick Tunnel لا يحتاج حتى domain)

### هل أحتاج domain؟
- Quick Tunnel: ❌ لا
- Named Tunnel: ✅ نعم

### هل HTTPS آمن؟
✅ نعم، مقبول من Telegram وجميع الخدمات

### كم حجم التحميل؟
- الحزمة المضغوطة: ~50KB (ملفات التكوين فقط)
- Docker images: ~500MB (تنزل تلقائياً عند التشغيل)

---

## 📚 المزيد من المساعدة

راجع [README.md](README.md) للحصول على:
- دليل شامل مفصل
- استكشاف الأخطاء وحلها
- أمثلة على الاستخدام مع Telegram
- نصائح أمنية

---

## 🆘 الدعم

إذا واجهت مشاكل:

1. تحقق من السجلات:
   ```bash
   docker-compose logs
   ```

2. تأكد من تشغيل Docker:
   ```bash
   docker ps
   ```

3. راجع قسم "استكشاف الأخطاء" في README.md

---

**صُنع بـ ❤️ للمجتمع العربي**

**الإصدار:** 1.0.0  
**تاريخ الإنشاء:** 2026-02-04
