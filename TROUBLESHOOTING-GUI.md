# 🔧 حل مشاكل الواجهة الرسومية

## المشكلة: الشاشة السوداء تظهر وتختفي

### الحل 1: استخدام الطريقة البديلة ⭐

انقر بالزر الأيمن على **`setup-gui.ps1`** واختر **"Run with PowerShell"**

---

### الحل 2: استخدام RUN-GUI.ps1

انقر بالزر الأيمن على **`RUN-GUI.ps1`** واختر **"Run with PowerShell"**

---

### الحل 3: تغيير سياسة التنفيذ

1. افتح **PowerShell كـ Administrator** (انقر بالزر الأيمن → Run as administrator)
2. شغّل الأمر:
```powershell
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```
3. اضغط `Y` للموافقة
4. جرب تشغيل `n8n-Installer.bat` مرة أخرى

---

### الحل 4: التشغيل اليدوي

افتح **PowerShell** في مجلد المشروع وشغّل:
```powershell
.\setup-gui.ps1
```

---

## المشكلة: "Cannot be loaded because running scripts is disabled"

هذه مشكلة سياسة التنفيذ. استخدم **الحل 3** أعلاه.

---

## المشكلة: الواجهة لا تظهر نهائياً

1. تأكد من أنك في المجلد الصحيح (حيث توجد الملفات)
2. تأكد من تثبيت PowerShell 5.1+ (موجود افتراضياً في Windows 10/11)
3. جرب إعادة تشغيل Windows

---

## البديل: استخدام سطر الأوامر (بدون GUI)

إذا لم تعمل الواجهة، يمكنك استخدام الطريقة القديمة:

1. انسخ `.env.example` إلى `.env`
2. افتح `.env` بـ Notepad وعدّله
3. شغّل:
```powershell
.\quick-start.ps1
```

---

## اتصل بالدعم

إذا لم ينجح أي حل، أرسل screenshot للخطأ.
