# 📦 دليل توزيع الصور Offline

## 🎯 الهدف
توفير صور Docker جاهزة للمستخدمين بدون الحاجة لتحميلها من الإنترنت.

---

## 📋 خطوات التوزيع

### للمطور (أنت):

#### 1. حفظ الصور

**Linux/macOS:**
```bash
./save-images.sh
```

**Windows:**
```powershell
.\save-images.ps1
```

**النتيجة:**
- ملف `n8n-images.tar.gz` (~350-400 MB)
- يحتوي على n8n + cloudflared

#### 2. رفع الملف

يمكنك:
- رفعه على Google Drive / Dropbox / OneDrive
- رفعه على موقعك
- توزيعه على USB
- رفعه على GitHub Releases (إذا < 2GB)

---

### للمستخدم النهائي:

#### 1. تحميل الملف

يحمّل `n8n-images.tar.gz` ويضعه في مجلد المشروع.

#### 2. استيراد الصور

**Linux/macOS:**
```bash
./load-images.sh
```

**Windows:**
```powershell
.\load-images.ps1
```

#### 3. التشغيل

```bash
./quick-start.sh  # أو .\quick-start.ps1
```

**لن يحتاج تحميل من الإنترنت!** ⚡

---

## ✅ المميزات

| الميزة | التفاصيل |
|--------|----------|
| **Offline** | لا يحتاج إنترنت بعد التحميل |
| **سريع** | تحميل محلي أسرع بكثير |
| **موثوق** | لا يعتمد على Docker Hub |
| **مضغوط** | ~350-400 MB فقط |

---

## 📊 الحجم المتوقع

```
n8n image:               ~300 MB
cloudflared image:       ~50 MB
Compression (gzip):      -20%
──────────────────────────────
Final size:              ~350-400 MB
```

---

## 🔄 الخيار البديل: Docker Hub

إذا أردت رفع صورة مخصصة على Docker Hub:

### 1. بناء صورة مخصصة

```bash
docker build -t yourusername/n8n-ready:latest .
```

### 2. رفعها

```bash
docker push yourusername/n8n-ready:latest
```

### 3. المستخدمون يحملونها

```bash
docker pull yourusername/n8n-ready:latest
```

**لكن:** الصور الرسمية (n8nio/n8n) أفضل للتحديثات الأمنية.

---

## 💡 التوصية

**استخدم طريقة .tar.gz** إذا:
- ✅ المستخدمون لديهم إنترنت بطيء
- ✅ تريد توزيع offline
- ✅ تريد إصدار ثابت ومضمون

**استخدم Docker Hub العادي** إذا:
- ✅ المستخدمون لديهم إنترنت جيد
- ✅ تريد آخر التحديثات دائماً
- ✅ لا تريد ملف كبير للتوزيع

---

## 🚀 البدء السريع

```bash
# 1. احفظ الصور (مرة واحدة)
./save-images.sh

# 2. وزع الملف مع الحزمة

# 3. المستخدم يستورد
./load-images.sh

# 4. يشغّل
./quick-start.sh
```

**Done!** 🎉
