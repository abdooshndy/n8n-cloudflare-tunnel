# 🪟 دليل Windows السريع - n8n مع Cloudflare Tunnel

## ⚡ البدء السريع (3 خطوات فقط!)

### الخطوة 1️⃣: تثبيت Docker Desktop

1. **حمّل Docker Desktop**:
   - افتح: https://www.docker.com/products/docker-desktop/
   - انقر على "Download for Windows"

2. **ثبّت البرنامج**:
   - شغّل الملف المحمل `Docker Desktop Installer.exe`
   - اتبع خطوات التثبيت
   - **مهم**: فعّل خيار "Use WSL 2 instead of Hyper-V"

3. **أعد تشغيل الحاسوب** (إذا طُلب منك)

4. **شغّل Docker Desktop**:
   - ابحث عن "Docker Desktop" في قائمة Start
   - شغّل البرنامج
   - انتظر حتى تصبح أيقونة Docker باللون الأخضر في System Tray

---

### الخطوة 2️⃣: الإعداد والتشغيل (الطريقة الجديدة السهلة 🛡️)

1. **انقر مرتين** على ملف `Start-n8n.bat`
2. **إذا كان أول مرة:**
   - سيقوم السكريبت بفتح صفحة الإعداد في المتصفح تلقائياً.
   - املأ البيانات واضغط **Download Configuration**.
   - احفظ ملف `.env` في نفس المجلد.
   - اضغط أي زر في الشاشة السوداء للمتابعة.

3. **إذا كانت البيانات موجودة:**
   - سيقوم السكريبت بالتحقق من Docker.
   - يقوم بتحميل الصور (أو بنائها) تلقائياً.
   - يشغل النظام ويعطيك الرابط في النهاية! 🚀

---

### ملاحظة هامة:
**لا تحتاج** لتشغيل `n8n-Installer.bat` يدوياً، السكريبت الرئيسي `Start-n8n.bat` يقوم بكل شيء!

---

### الخطوة 3️⃣: الحصول على رابط n8n

سيظهر الرابط **تلقائياً** في نهاية تشغيل `Start-n8n.bat`.
إذا أردت الحصول عليه لاحقاً:

1. **انقر مرتين** على `Start-n8n.bat` مرة أخرى (لن يعيد التشغيل بل سيعطيك الرابط إذا كان يعمل).
2. أو استخدم `get-url.ps1`.

---

## 🎯 الاستخدام اليومي

### تشغيل n8n:
فقط انقر مرتين على `Start-n8n.bat` 🚀

### إيقاف n8n:
فقط أغلق النافذة السوداء (أو اضغط Ctrl+C فيها).

### الحصول على الرابط:
انظر للشاشة السوداء أو شغل `Starts-n8n.bat` مرة أخرى.

---

## 🔧 استكشاف الأخطاء (Windows)

### المشكلة: "WSL needs updating" في Docker Desktop ⭐

![WSL Update Required](https://i.imgur.com/placeholder.png)

**الحل التلقائي (الأسهل):**

1. **انقر بالزر الأيمن** على `FIX-WSL.bat`
2. اختر **"Run as administrator"**
3. انتظر حتى ينتهي التحديث
4. أعد تشغيل Docker Desktop
5. شغّل `Start-n8n.bat` مرة أخرى

**الحل اليدوي:**

```powershell
# افتح PowerShell كـ Administrator
wsl --update
wsl --shutdown
```

ثم أعد تشغيل Docker Desktop

---

### المشكلة: "docker: command not found"

**الحل:**
1. تأكد من تثبيت Docker Desktop
2. تأكد من تشغيل Docker Desktop (أيقونة في System Tray)
3. أعد تشغيل الحاسوب

---

### المشكلة: Docker Desktop لا يبدأ

**الحل:**
1. تأكد من تفعيل WSL 2:
   - افتح PowerShell كـ Administrator
   - شغّل: `wsl --install`
   - أعد تشغيل الحاسوب

---

### المشكلة: "docker: command not found"

**الحل:**
1. تأكد من تثبيت Docker Desktop
2. تأكد من تشغيل Docker Desktop (أيقونة في System Tray)
3. أعد تشغيل PowerShell

---

### المشكلة: "Cannot run scripts"

**الحل:**
```powershell
# افتح PowerShell كـ Administrator
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

ثم حاول مرة أخرى

---

### المشكلة: Docker Desktop لا يبدأ

**الحل:**
1. تأكد من تفعيل WSL 2:
   - افتح PowerShell كـ Administrator
   - شغّل: `wsl --install`
   - أعد تشغيل الحاسوب

2. تحديث Windows:
   - Settings → Windows Update
   - ثبّت جميع التحديثات

---

### المشكلة: "Access Denied" عند تشغيل Docker

**الحل:**
1. شغّل PowerShell كـ **Administrator**
2. أو أضف المستخدم الحالي لمجموعة docker-users:
   - Docker Desktop → Settings → Resources → WSL Integration

---

## 📝 ملاحظات مهمة لمستخدمي Windows

### ✅ نصائح:

1. **استخدم PowerShell** (وليس Command Prompt)
2. **شغّل Docker Desktop** قبل تشغيل السكريبتات
3. **انتظر** حتى تصبح أيقونة Docker خضراء
4. **النقر بالزر الأيمن** على ملفات `.ps1` واختر "Run with PowerShell"

---

### ⚠️ تحذيرات:

1. **لا تغلق** Docker Desktop أثناء تشغيل n8n
2. **احفظ الرابط** إذا كنت تستخدم Quick Tunnel (يتغير عند إعادة التشغيل)
3. **اصنع نسخة احتياطية** من مجلد `n8n-data` بانتظام

---

## 🎥 فيديو توضيحي (خطوة بخطوة)

### 1. تثبيت Docker Desktop
```
1. افتح الرابط
2. حمّل الملف
3. ثبّت البرنامج
4. أعد تشغيل Windows
5. شغّل Docker Desktop
```

### 2. تشغيل n8n
```
1. انقر مرتين على START-WINDOWS.bat
2. اتبع التعليمات
3. أو انقر بالزر الأيمن على quick-start.ps1
4. اختر Run with PowerShell
```

### 3. الحصول على الرابط
```
1. انقر بالزر الأيمن على get-url.ps1
2. اختر Run with PowerShell
3. انسخ الرابط
4. افتحه في المتصفح
```

---

## 🌐 فتح PowerShell في المجلد

### الطريقة 1 (Windows 11):
1. افتح مجلد المشروع
2. انقر بالزر الأيمن على مكان فارغ
3. اختر "Open in Terminal"

### الطريقة 2 (Windows 10):
1. افتح مجلد المشروع
2. اضغط `Shift` + **انقر بالزر الأيمن**
3. اختر "Open PowerShell window here"

### الطريقة 3 (من شريط العنوان):
1. افتح مجلد المشروع
2. انقر في شريط العنوان (Address Bar)
3. اكتب `powershell` واضغط Enter

---

## 🔐 الأمان على Windows

### حماية الملفات:

- ❌ **لا تشارك** ملف `.env`
- ✅ **احتفظ بنسخة احتياطية** من `n8n-data`
- ✅ **استخدم Windows Defender** أو برنامج حماية

---

## 📦 النسخ الاحتياطي على Windows

### نسخ يدوي:
```
1. أوقف الخدمات (docker-compose down)
2. انسخ مجلد n8n-data
3. انسخ ملف .env
4. احفظهم في مكان آمن
```

### استخدام برنامج:
- **7-Zip**: ضغط المجلد
- **Windows Backup**: نسخ احتياطي تلقائي

---

## ❓ الأسئلة الشائعة (Windows)

### هل يعمل على Windows 10؟
✅ نعم، Windows 10 (64-bit) مدعوم

### هل أحتاج WSL 2؟
✅ نعم، Docker Desktop يحتاجه (يُثبت تلقائياً)

### كم يستهلك من الموارد؟
- RAM: ~2-4 GB
- Disk: ~2-3 GB

### هل يعمل في الخلفية؟
✅ نعم، Docker Desktop يعمل كخدمة Windows

---

## 🆘 الدعم

### إذا واجهت مشاكل:

1. راجع قسم "استكشاف الأخطاء" أعلاه
2. تحقق من سجلات Docker:
   ```powershell
   docker-compose logs
   ```
3. راجع الدليل الكامل: `README.md`

---

## 🎯 الخلاصة لمستخدمي Windows

**التثبيت:**
1. Docker Desktop → تثبيت
2. Start-n8n.bat → تشغيل (يقوم بالإعداد والبدء)
3. المتصفح → استمتع!

**الاستخدام:**
- get-url.ps1 → للحصول على الرابط
- المتصفح → افتح الرابط
- ابدأ العمل! 🎉

---

**سهل وبسيط! 😊**
