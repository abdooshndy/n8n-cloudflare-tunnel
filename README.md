# دليل تنصيب n8n مع Cloudflare Tunnel

[![GitHub](https://img.shields.io/badge/GitHub-Repository-181717?logo=github)](https://github.com/abdooshndy/n8n-cloudflare-tunnel)

## 🔗 روابط سريعة
- **المستودع الرسمي:** [github.com/abdooshndy/n8n-cloudflare-tunnel](https://github.com/abdooshndy/n8n-cloudflare-tunnel)
- **التحميل (Releases):** [صفحة التحميل](https://github.com/abdooshndy/n8n-cloudflare-tunnel/releases)

---

## 📋 نظرة عامة

هذا المشروع يوفر إعداد كامل لتشغيل **n8n** (أداة أتمتة سير العمل) داخل Docker مع **Cloudflare Tunnel** للحصول على اتصال HTTPS آمن **بدون الحاجة لـ IP ثابت**.

### ✨ المميزات

- ✅ **بدون IP ثابت** - يعمل حتى مع IP ديناميكي
- ✅ **HTTPS مجاني** - شهادات SSL تلقائية من Cloudflare
- ✅ **خيار مجاني 100%** - Quick Tunnel بدون الحاجة لـ domain!
- ✅ **متعدد المنصات** - يعمل على Linux, Windows, macOS
- ✅ **آمن** - اتصال مشفر + مصادقة أساسية (مقبول من Telegram وجميع الخدمات)
- ✅ **سهل النقل** - كل شيء في مجلد واحد

---

## 🆓 خياران للاستخدام

### الخيار 1: Quick Tunnel (موصى به للمبتدئين)

| الميزة | التفاصيل |
|-------|----------|
| **التكلفة** | 🆓 **مجاني 100%** |
| **Domain مطلوب** | ❌ لا يحتاج |
| **حساب Cloudflare** | ❌ لا يحتاج |
| **HTTPS** | ✅ نعم، آمن تماماً |
| **الرابط** | عشوائي مثل: `https://abc-xyz.trycloudflare.com` |
| **ملاحظة** | الرابط يتغير عند إعادة التشغيل |

**مثالي لـ**: التجربة، التطوير، الاستخدام الشخصي

### الخيار 2: Named Tunnel (للاستخدام الاحترافي)

| الميزة | التفاصيل |
|-------|----------|
| **التكلفة** | 🆓 مجاني (لكن يحتاج domain) |
| **Domain مطلوب** | ✅ نعم |
| **حساب Cloudflare** | ✅ نعم |
| **HTTPS** | ✅ نعم، آمن تماماً |
| **الرابط** | ثابت تختاره: `https://n8n.yourdomain.com` |
| **ملاحظة** | الرابط لا يتغير أبداً |

**مثالي لـ**: الإنتاج، الاستخدام التجاري، الفرق

---

## 📦 المتطلبات الأساسية

### 1. تثبيت Docker و Docker Compose

#### Linux (Ubuntu/Debian):
```bash
# تحديث النظام
sudo apt update && sudo apt upgrade -y

# تثبيت Docker
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh

# تثبيت Docker Compose
sudo apt install docker-compose -y

# إضافة المستخدم الحالي لمجموعة docker
sudo usermod -aG docker $USER
newgrp docker
```

#### Windows:
1. تحميل وتثبيت [Docker Desktop for Windows](https://www.docker.com/products/docker-desktop/)
2. تشغيل Docker Desktop
3. التأكد من تفعيل WSL 2

#### macOS:
1. تحميل وتثبيت [Docker Desktop for Mac](https://www.docker.com/products/docker-desktop/)
2. تشغيل Docker Desktop

### 2. للخيار الثاني فقط (Named Tunnel)

- حساب مجاني على [Cloudflare](https://www.cloudflare.com)
- نطاق (Domain) مسجل ومضاف إلى Cloudflare

---

## 🚀 خطوات التنصيب

### ⚡ الطريقة السريعة (موصى بها)

```bash
# انتقل لمجلد المشروع
cd "/media/abdoo/New Volume/n8n"

# شغّل السكريبت
./quick-start.sh
```

السكريبت سيسألك:
1. أي نوع tunnel تريد (Quick أو Named)
2. سيقوم بكل شيء تلقائياً!

---

### 📖 الطريقة اليدوية

#### الخطوة 1: إعداد ملف البيئة

```bash
# انسخ ملف المثال
cp .env.example .env

# افتح الملف للتحرير
nano .env
```

**عدّل المعلومات التالية:**

```env
# بيانات تسجيل الدخول (غيّرها!)
N8N_USER=admin
N8N_PASSWORD=your_very_strong_password_here

# المنطقة الزمنية
TIMEZONE=Africa/Cairo
```

#### الخطوة 2: تشغيل الخدمة

```bash
# تشغيل الحاوية الموحدة
docker-compose up -d

# عرض الرابط (Quick Tunnel)
docker-compose logs n8n | grep "https://"
```

---

## 🔧 الأوامر المفيدة

### إدارة الخدمات

```bash
# تشغيل
docker-compose up -d

# إيقاف
docker-compose down

# السجلات
docker-compose logs n8n

# عرض الرابط (Quick Tunnel)
docker-compose logs n8n | grep "https://"

# الحالة
docker-compose ps
```

### الصيانة

```bash
# إعادة بناء الصورة (في حال تحديث الكود)
docker-compose build
docker-compose up -d

# إعادة التشغيل
docker-compose restart
```

---

## 💾 النسخ الاحتياطي

### نسخ احتياطي من البيانات

```bash
# إنشاء نسخة احتياطية مضغوطة
tar -czf n8n-backup-$(date +%Y%m%d).tar.gz n8n-data/ .env

# أو نسخ المجلد مباشرة
cp -r n8n-data n8n-data-backup
```

### استعادة من النسخة الاحتياطية

```bash
# إيقاف الخدمات
docker-compose down

# استعادة البيانات
tar -xzf n8n-backup-YYYYMMDD.tar.gz

# تشغيل الخدمات
docker-compose up -d
```

---

## 🔍 استكشاف الأخطاء وحلها

### المشكلة: لا أرى الرابط في Quick Tunnel

```bash
# انتظر 10 ثوانٍ ثم:
docker-compose logs cloudflared-quick-tunnel | grep -i "https://"

# أو شاهد السجلات مباشرة:
docker-compose logs -f cloudflared-quick-tunnel
```

### المشكلة: الكونتينر لا يعمل

```bash
# التحقق من السجلات للأخطاء
docker-compose logs n8n

# التحقق من صحة ملف .env
cat .env
```

### المشكلة: خطأ في المصادقة

- تأكد من أن `N8N_USER` و `N8N_PASSWORD` في ملف `.env` صحيحة
- جرب مسح كاش المتصفح أو استخدام وضع التصفح الخاص

### المشكلة: تغيّر الرابط في Quick Tunnel

هذا طبيعي! الرابط يتغير عند إعادة التشغيل. للحصول على رابط ثابت:
- استخدم Named Tunnel مع domain
- أو استخدم خدمة مثل ngrok Pro

---

## 🔗 كيفية الحصول على رابط Quick Tunnel

إذا كنت تستخدم **Quick Tunnel**، الرابط يتغير في كل مرة تعيد تشغيل الخدمات. إليك **4 طرق** للحصول على الرابط:

### الطريقة 1: استخدام السكريبت المخصص (الأسهل) ⭐

#### Linux/macOS:
```bash
./get-url.sh
```

#### Windows:
```powershell
.\get-url.ps1
```

**النتيجة:**
```
============================================
✅ تم العثور على الرابط!
============================================

🌐 رابط n8n الخاص بك:

   https://abc-xyz-123.trycloudflare.com

============================================
```

---

### الطريقة 2: من السجلات مباشرة

```bash
docker-compose logs n8n | grep "https://"
```

**مثال على النتيجة:**
```
https://random-name-abc.trycloudflare.com
```

---

### الطريقة 3: مشاهدة السجلات الحية

```bash
docker-compose logs -f n8n
```

---

### الطريقة 4: أمر واحد سريع

```bash
# Linux/macOS
docker-compose logs n8n 2>&1 | grep -o 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' | tail -1

# Windows (PowerShell)
docker-compose logs n8n 2>&1 | Select-String -Pattern 'https://[a-zA-Z0-9.-]*\.trycloudflare\.com' | Select-Object -Last 1
```

---

### 💡 نصائح مهمة

> [!TIP]
> **احفظ الرابط في مكان آمن** أثناء استخدامه (مثلاً في ملف نصي أو في Telegram)

> [!WARNING]
> **الرابط يتغير فقط عند**:
> - إعادة تشغيل الكونتينر (`docker-compose restart`)
> - إيقاف ثم تشغيل الخدمات (`down` ثم `up`)
> 
> **الرابط لا يتغير** إذا تركت الكونتينر يعمل بشكل مستمر

> [!NOTE]
> إذا كنت تحتاج رابط ثابت لا يتغير أبداً، استخدم **Named Tunnel** مع domain

---

### 📱 استخدام الرابط مع Telegram

بعد الحصول على الرابط (مثلاً: `https://abc-xyz.trycloudflare.com`):

1. في n8n، أنشئ webhook node
2. استخدم الرابط كـ webhook URL في Telegram Bot:
   ```
   https://abc-xyz.trycloudflare.com/webhook/telegram
   ```

3. **عند إعادة التشغيل**:
   - احصل على الرابط الجديد بأي من الطرق أعلاه
   - حدّث webhook URL في Telegram Bot

---

## 🌍 النقل إلى نظام تشغيل آخر

### من Linux إلى Windows/macOS (أو العكس)

1. **انسخ المجلد بالكامل** إلى النظام الجديد
2. **تأكد من تثبيت Docker** على النظام الجديد
3. **شغّل الخدمات**:
   ```bash
   docker-compose up -d
   ```

---

## 🔗 الربط مع Telegram والخدمات الأخرى

عند استخدام **Quick Tunnel** أو **Named Tunnel**، ستحصل على HTTPS آمن مقبول من جميع الخدمات:

### Telegram Bot Webhook

```
https://your-url.trycloudflare.com/webhook/telegram
```

**أو مع Named Tunnel:**
```
https://n8n.yourdomain.com/webhook/telegram
```

✅ **HTTPS** - مقبول من Telegram
✅ **آمن** - مشفر بالكامل
✅ **يعمل** - بدون مشاكل

> [!TIP]
> إذا كنت تستخدم Quick Tunnel، احفظ الرابط الجديد في Telegram عند كل إعادة تشغيل

---

## 📚 موارد إضافية

- [n8n Documentation](https://docs.n8n.io/)
- [Cloudflare Tunnel Docs](https://developers.cloudflare.com/cloudflare-one/connections/connect-apps/)
- [Docker Documentation](https://docs.docker.com/)

---

## 🔒 ملاحظات أمنية

> [!CAUTION]
> - **لا تشارك ملف `.env`** مع أحد - يحتوي على معلومات حساسة
> - استخدم **كلمة مرور قوية** لـ n8n
> - احتفظ **بنسخ احتياطية منتظمة** من مجلد `n8n-data`
> - لا ترفع ملف `.env` إلى Git (محمي بـ `.gitignore`)

---

## ❓ الأسئلة الشائعة

### هل Quick Tunnel آمن؟
✅ نعم! يوفر HTTPS كامل ومقبول من جميع الخدمات

### هل الرابط ثابت في Quick Tunnel؟
❌ لا، يتغير عند إعادة التشغيل. للحصول على رابط ثابت استخدم Named Tunnel

### هل أحتاج domain؟
- Quick Tunnel: ❌ لا
- Named Tunnel: ✅ نعم

### كم التكلفة؟
🆓 **مجاني 100%** في كلا الحالتين! (Named Tunnel يحتاج domain فقط)

---

**صُنع بـ ❤️ للمجتمع العربي**
