# ===========================================
# n8n Configuration
# ===========================================

# اسم المستخدم وكلمة المرور لـ n8n
# n8n username and password
N8N_USER=admin
N8N_PASSWORD=change_this_to_a_strong_password

# المنطقة الزمنية / Timezone
# أمثلة: Africa/Cairo, Europe/London, America/New_York, Asia/Dubai
TIMEZONE=Africa/Cairo

# ===========================================
# اختر واحد من الخيارين التاليين:
# Choose ONE of the following options:
# ===========================================

# ----- الخيار 1: Quick Tunnel (مجاني، بدون domain) -----
# ----- Option 1: Quick Tunnel (Free, no domain needed) -----
# 
# ببساطة اترك المتغيرات التالية كما هي واستخدم الأمر:
# Simply leave the variables below as-is and use the command:
#
#   docker-compose --profile quick-tunnel up -d
#
# ستحصل على رابط HTTPS عشوائي مثل:
# You'll get a random HTTPS URL like:
#   https://random-name.trycloudflare.com
#
# الرابط يتغير عند إعادة التشغيل، لكن HTTPS مضمون!
# The URL changes on restart, but HTTPS is guaranteed!

N8N_HOST=your-tunnel-url.trycloudflare.com
WEBHOOK_URL=https://your-tunnel-url.trycloudflare.com

# ----- الخيار 2: Named Tunnel (مع domain خاص) -----
# ----- Option 2: Named Tunnel (With custom domain) -----
#
# إذا كان لديك domain مسجل في Cloudflare:
# If you have a domain registered with Cloudflare:
#
# 1. قم بإنشاء Named Tunnel من Cloudflare Dashboard
# 2. احصل على التوكن
# 3. ألغِ التعليق عن السطر التالي وضع التوكن:
# 1. Create a Named Tunnel from Cloudflare Dashboard
# 2. Get the token
# 3. Uncomment the line below and paste the token:

# CLOUDFLARE_TUNNEL_TOKEN=eyJhIjoixxxxxxxxxxxxxxxx...

# 4. غيّر N8N_HOST و WEBHOOK_URL أعلاه إلى نطاقك:
# 4. Change N8N_HOST and WEBHOOK_URL above to your domain:
#    N8N_HOST=n8n.yourdomain.com
#    WEBHOOK_URL=https://n8n.yourdomain.com

# 5. استخدم الأمر:
# 5. Use the command:
#    docker-compose --profile named-tunnel up -d

# ===========================================
# ملاحظات / Notes
# ===========================================
# - لا تشارك هذا الملف مع أحد / Don't share this file
# - غيّر كلمة المرور! / Change the password!
# - Quick Tunnel: لا يحتاج token / No token needed
# - Named Tunnel: يحتاج token + domain / Needs token + domain
